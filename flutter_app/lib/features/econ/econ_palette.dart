import 'package:flutter/material.dart';

abstract final class EconPalette {
  static const primary = Color(0xFFFFC067);
  static const primaryDark = Color(0xFFE29A3A);
  static const softPrimary = Color(0xFFFFF1DC);
  // Selected controls use a quiet khaki so the orange subject accent remains
  // visible on both the frame and the icon blocks.
  static const highlight = Color(0xFFF7E9BE);
  static const pale = Color(0xFFFFF9EF);
  static const ink = Color(0xFF5D4037);
  static const border = Color(0xFFF5D09A);
}
