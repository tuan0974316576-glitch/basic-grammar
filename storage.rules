rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /vocab-audio/{allPaths=**} {
      allow get: if true;
      allow list: if false;
      allow write: if false;
    }

    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
